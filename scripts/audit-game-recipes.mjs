import fs from 'node:fs';
import {fileURLToPath} from 'node:url';
import {loadRuntimeIntegrationEngine} from '../tests/helpers/runtime-catalog-loader.mjs';
import rules from './recipe-rules.js';
const root=new URL('../',import.meta.url),engine=loadRuntimeIntegrationEngine(root,{fetch:async url=>{
 const data=fs.readFileSync(new URL(String(url).replace(/^\.\//,''),root));return {ok:true,status:200,text:async()=>data.toString(),json:async()=>JSON.parse(data)};
}});
engine.setState({items:engine.catalogs.items});await engine.itemsApi.load();const asset=await engine.recipesApi.load(),recipes=engine.recipesApi.rows(),groups=engine.recipesApi.groups();
const counts={};for(const raw of asset.recipes){const kind=rules.scope(raw).kind;counts[kind]=(counts[kind]||0)+1;}
const outputs=recipes.filter(r=>!r.dye).map(r=>({recipe:r,resultId:engine.recipesApi.defaults(r,null).resultId}));
await engine.itemsApi.hydrate([...new Set(outputs.map(r=>r.resultId).filter(id=>id.startsWith('bg3:')))]);
const outputCounts={},outputStatuses=new Map();for(const row of outputs){const status=engine.recipesApi.outputStatus(engine.itemsApi.resolve(row.resultId));outputStatuses.set(row.recipe.id,status);outputCounts[status.state]=(outputCounts[status.state]||0)+1;}
const audit=JSON.parse(fs.readFileSync(new URL('docs/recipe-item-audit.json',root),'utf8')),byId=new Map(audit.items.map(row=>[row.id,row]));
if(audit.counts.recipes!==recipes.length||outputs.some(row=>!byId.has(row.resultId)))throw new Error('Regenerate scripts/audit-recipe-items.mjs first.');
const clean=x=>String(x||'').replace(/\|/g,'/').replace(/\n/g,' ');
let report=`# Проверка рецептов игры\n\nВ [проверке каждого предмета](recipe-item-audit.json) перечислены все ${audit.counts.items} самостоятельных компонентов, инструментов и результатов. Отсутствующих определений: ${audit.counts.missingDefinitions}; ошибок структуры: ${audit.counts.invalidDomains}; отсутствующих файлов иллюстраций: ${audit.counts.missingIllustrations}. Точная общедоступная карточка найдена для ${audit.counts.exactPublicItems} предметов. Не прошли проверку полноты: ${audit.counts.itemsNeedingWork}. Помимо проверки структуры и подготовки, игровые последствия проверяются сценариями tests/recipe-tabletop.test.mjs и tests/game-recipes.test.mjs. Сорок ранее проблемных случаев получили [проверенные настольные правила](RECIPE_TABLETOP_RULES.md). Два из них объединены с существующими базовым ядом и зельем героизма, поэтому самостоятельных предметов стало 297 вместо 299.\n\nПроверено ${asset.recipes.length+engine.recipesApi.local.length} записей: ${recipes.length} способов изготовления, объединённых в ${groups.length} карточек по общему результату. У каждой карточки есть название, иллюстрация, состав, количества и правила расхода. Разные способы изготовления одного предмета сохранены внутри общей карточки. Пять ранее согласованных пар формул используют общие настольные предметы. Дополнительный рецепт большого лечения также создаёт существующее зелье с 4d4 + 4 лечения. Обычные клинки связаны с существующими предметами игры; старые экземпляры остаются пригодными для изготовления. Три эквивалентных сломанных механизма представлены одним компонентом. Основания выбора сохранены в [проверке связей](recipe-item-link-decisions.json).\n\nВ исходных данных сохранены ${counts.scene} взаимодействия с объектами и событиями, ${counts.unresolved} записи с отсутствующим ингредиентом и ${counts.template} служебный шаблон. Они не выдаются за доступные способы изготовления. Исходные идентификаторы и ссылки событий не удалены.\n\n## Игровые правила\n\nВсе рецепты используют общий цикл проверки, единого расхода, последствий и подтверждённого сохранения. Проверяются изучение формулы, компоненты, конкретный экземпляр, инструменты и владение, место работы, ритуальные ячейки и отсутствие активного боя. Окрашивание сохраняет свойства и экземпляр; преобразование отделяет одну основу от стопки. Отмена до подтверждения ничего не меняет. Повторное подтверждение не повторяет действие; ошибка сохранения откатывает мир. Время изготовления проходит для всего мира, включая длительные эффекты.\n\nДля ${recipes.filter(r=>r.durationMinutes==null).length} способов исходные данные не определяют время. Мастер обязан явно указать минуты, в том числе 0, если время не должно продвигаться. Неуказанные броски, СЛ и длительности не выдумываются. Название, заметки, избранное и время редактируются одинаково и сохраняются вместе с игрой.\n\n## Применение изготовленных предметов\n\nИзготовление и применение результата проверяются отдельно. Среди ${outputs.length} рецептов с новым предметом: ${outputCounts.ready||0} готовых определений, ${outputCounts.partial||0} частично доступных, ${outputCounts.blocked||0} заблокированных. Проверен результат по умолчанию каждого рецепта. Метаданные дополнены отдельной проверкой каждой канонической ссылки и сценариями реального применения. Уникальные изделия получили явные правила кампании на основе настольной D&D 5e (2014), сохраняя отличительные эффекты. Исходные программы сохранены в архиве; их неподдержанные ветви не объявлены исполненными.\n\nВсе компоненты, инструменты и результаты входят в единую базу предметов игры. При загрузке сохранений добавляются новые определения и переносятся 51 старый идентификатор: инвентари и вложенные сумки, предметы на земле, торговцы и добыча сундуков. Количество, настройка, стоимость и заметки экземпляров сохраняются; собственные отредактированные определения не перезаписываются. Исторические квитанции действий не переписываются.\n\nОбщие яды используют настольный способ доставки: слизь — контакт с кожей, «Злоба» — вдыхаемый яд в кубе 1,5 м, яд дроу — первое ранение обработанным предметом. Для взрывов и периодического урона игрок вводит кости; до разрешения ожидающих последствий ход или время не пропускаются. Споры не позволяют бесконечно обновлять дополнительную атаку, а дополнительные ячейки эликсиров расходуются отдельно в обоих режимах магии.\n\n## Проверка каждого способа\n\nРегрессия tests/game-recipes.test.mjs изготавливает каждый из ${recipes.length} способов в настоящем движке, проверяет состав, результат, время и повторное подтверждение. Дополнительные сценарии проверяют неудачное сохранение, устаревший план, одновременные подтверждения, окрашивание собственных предметов, преобразование стопок и особые экземпляры.\n\n| Идентификатор | Рецепт | Вид | Состав и операции | Время, мин. | Применение результата |\n|---|---|---|---|---|---|\n`;
for(const r of recipes)report+=`| ${r.id} | ${clean(r.name)} | ${rules.CATEGORIES[r.category]} | ${clean(r.inputs.map(i=>`${i.label} ×${i.qty} (${i.station?'место работы':i.operation})`).join('; '))} | ${r.durationMinutes??'задаёт мастер'} | ${r.dye?'свойства экипировки сохраняются':byId.get(r.resultIds[0])?.issues.length?'требует доработки':'ошибок при подготовке не обнаружено'} |\n`;
report+=audit.counts.itemsNeedingWork?'\n## Предметы с оставшимися проблемами\n\n| Предмет | Точная карточка | Необходимая проверка или реализация |\n|---|---|---|\n':'\n## Результат сверки\n\nНе найдено отсутствующих определений, разорванных ссылок, отсутствующих иллюстраций или заблокированных планов применения компонентов и результатов.\n';
for(const item of audit.items.filter(row=>row.issues.length)){
 const reasons=item.runtime.flatMap(row=>row.reason?[row.reason]:(row.issues||[]).map(issue=>issue.reason));
 report+=`| ${clean(item.name)} (${item.statsId}) | ${item.publicItemId?'есть':item.namesakeId?'есть другая одноимённая запись':'отсутствует'} | ${clean(reasons.length?reasons.join('; '):item.issues.join('; '))} |\n`;
}
report+='\n## Записи событий и шаблоны\n\n| Идентификатор | Классификация | Причина |\n|---|---|---|\n';
for(const raw of asset.recipes){const status=rules.scope(raw);if(status.kind!=='recipe')report+=`| ${raw.id} | ${status.kind} | ${status.reason} ${clean(raw.manualReasons?.join('; '))} |\n`;}
const output=new URL('docs/RECIPE_AUDIT.md',root);if(process.argv.includes('--check')){if(fs.readFileSync(output,'utf8')!==report)throw new Error('Recipe audit is outdated; run node scripts/audit-game-recipes.mjs');}else fs.writeFileSync(output,report);
console.log(JSON.stringify({recipes:recipes.length,cards:groups.length,disposition:counts,outputMetadata:outputCounts,itemsNeedingWork:audit.counts.itemsNeedingWork,report:fileURLToPath(output)}));
