# Каталог предметов BG3 и движок: открытый статус

Дата среза: **19 августа 2026 года**.  Этот файл — одновременно карта
локальных материалов, журнал выполненной работы и инструкция по независимой
проверке. Он намеренно не скрывает промежуточные результаты за внутренними
инструментами или сетевыми сервисами.

> **Статус среза:** каталог `bg3-24532579-v8` опубликован как неизменяемый
> набор данных. Корневой runtime `readBook` / `toggleLight` завершён для 2 934
> профильных строк: 1 467 на активный профиль, включая 1 218 чтений и 249
> переключений света на 1 466 предметах. Весь срез `standard` исполнен причинно,
> а `honour` имеет нулевую нормализованную семантическую разницу и отдельные
> точные Story/read/light/durability-квитанции. Частные runtime `Summon`, Arrow
> of Many Targets и Scroll of Dethrone также завершены и повторно проверены:
> `Summon` исполняет 18 из 20 typed-носителей, Arrow — 2 точные строки одного
> предмета, Dethrone — 2 профильные строки одного свитка. Неоднозначные соседние
> маршруты остаются fail-closed до оплаты. Хеши и результаты приведены ниже.

## Что доступно владельцу

Корень проекта:

```text
C:\Users\admin\.codex\visualizations\2026\08\12\019ff62f-b4c0-72f2-917b-9630506f8cc9\dnd-world
```

Все материалы текущего цикла находятся в обычных локальных файлах:

- `index.html` — пользовательский интерфейс и исполняемый движок 4.6;
- `data/bg3/current.json` — активный указатель на версию каталога;
- `data/bg3/bg3-24532579-v8/manifest.json` — манифест каталога, профили,
  контракты, количества, пути к сегментам и runtime-capabilities;
- `data/bg3/bg3-24532579-v8/items/*.json` — 814 сегментов полных записей
  предметов;
- `data/bg3/bg3-24532579-v8/item-rule-links/*.json` — 256 сегментов связей
  предметов с правилами;
- `data/bg3/bg3-24532579-v8/root-template-programs/` и
  `placement-action-programs/` — программы корневых шаблонов и действий
  размещённых объектов;
- `data/bg3/bg3-24532579-v8/rules/{spells,statuses,passives,interrupts}/` —
  исходные нормализованные правила;
- `data/bg3/bg3-24532579-v8/story-programs/`, `story-source/` и
  `story-items.json` — сюжетные программы, их происхождение и предметы;
- `data/bg3/bg3-24532579-v8/recipes.json`, `treasure-tables.json`,
  `item-placements.json`, `book-content.json` — рецепты, сокровища, размещения
  и содержимое книг;
- `data/bg3/bg3-24532579-v8/search-index.json`,
  `rule-program-index.json`, `rule-program-coverage.json` и
  `mechanics-coverage.json` — поисковый индекс и проверяемые отчёты покрытия;
- `data/bg3/bg3-24532579-v8/icon-manifest.json` и `assets/bg3/icons/` —
  сопоставление и локальные изображения;
- `tests/` — исполняемая спецификация поведения и точных переписей.

Активный указатель выбирает `bg3-24532579-v8`, сборку Steam `24532579`, и
профиль `standard` по умолчанию. Каталог содержит 10 284 предмета и 5 898
программ правил. SHA-256 манифеста, хранящийся в `current.json`, совпадает с
байтами `manifest.json`; это отдельный хеш **каталога**. Хеши зафиксированных
runtime-файлов приведены в разделе о совместной проверке корневых действий,
`Summon`, Arrow, Dethrone и A33.

Полный процесс получения данных тоже остаётся локальным и читаемым:

- `C:\Users\admin\.codex\visualizations\2026\08\12\019ff62f-b4c0-72f2-917b-9630506f8cc9\bg3-dndworld-stage1`
  содержит извлечение, исходные JSONL, перечни включённых и исключённых
  предметов, отчёты, `analyze_stage1.py`, `build_deliverables.py` и
  `validate_stage1.py`;
- `C:\Users\admin\.codex\visualizations\2026\08\12\019ff62f-b4c0-72f2-917b-9630506f8cc9\bg3-dndworld-stage2`
  содержит `build_bg3_catalog.py`, компилятор правил, аудит размещений и сцен,
  отчёты сборки, журналы, исходные иконки и Python-тесты публикации.

Таким образом, итоговый каталог, промежуточные данные, логика сборки, отчёты и
тесты доступны отдельно. Firebase и иная облачная синхронизация не нужны для
чтения каталога, локального запуска, экспорта кампании или тестирования.

## Что означает `private runtime proof`

`Private` в этом проекте означает **доказательство целостности внутри одной
операции**, а не контроль доступа:

- план связывается с точными предметом, действием, профилем, состоянием мира и
  введённым игроком результатом;
- оторванная копия, подмена полей, повторное использование токена или
  изменение мира между проверкой и коммитом отвергаются;
- ошибка после начала коммита откатывает уже подготовленные изменения;
- временные токены хранятся в замыкании/`WeakMap` и не сериализуются, поэтому
  импортированный файл не может случайно повторить старую транзакцию.

Это не шифрование, не DRM, не пароль, не лицензия и не ограничение прав
владельца. Исходный код, JSON, тесты, отчёты и экспорт кампании доступны для
чтения и редактирования. После импорта новая корректная операция создаёт новое
доказательство. В экспорт попадает устойчивое итоговое состояние мира, но не
одноразовые служебные полномочия незавершённого действия.

Запрет `Math.random` в игровых последствиях имеет ту же цель: результат
воспроизводим, а значения костей вводят живые игроки. Это также не ограничение
доступа.

## Завершённые срезы и точные переписи

| Срез | Точный охват | Текущее поведение |
| --- | ---: | --- |
| Formula / лечение | 12 строк: 6 предметов × 2 профиля | Полная частная граница ввода формулы и единичного коммита. Остальные неоднозначные ветви формул не получают частичного эффекта. |
| A33 `LearnSpell` | 228 строк = 144 готовых + 84 заблокированных | Готовые строки учат заклинанию с точной стоимостью и единичным расходом; каждая блокировка имеет причину до оплаты. |
| A23 Recipe | 398 маршрутов; 548 объявленных ссылок = 410 допустимых + 138 недопустимых | Выбор рецепта, входы, трансформации и результат проверяются до единого коммита; недопустимая ссылка не исполняется. |
| Root `readBook` / `toggleLight` | 2 934 профильные строки = 1 467 на профиль: 1 218 чтений + 249 переключений света; 1 466 предметов и 10 Story-связей на активный профиль | Все 1 467 строки `standard` проходят production-маршрут; `honour` имеет точное семантическое зеркало. Подготовка и отмена не меняют мир, коммит единичный, свет получает детерминированную source-unique идентичность, а Story, rollback/retry/replay и сохранение входят в причинный контракт. |
| `Summon` | 144 program/profile-строки; 34 item/action/profile-строки = 17 предметов: 20 typed (10 предметов) + 14 mixed (7 предметов) | Зафиксированная ограниченная матрица исполняет 18 из 20 typed-строк (9 на профиль); 2 строки Scrying отклоняются из-за несовпадающего `CanStand`; все 14 mixed-строк остаются закрытыми. |
| Arrow of Many Targets | 46 правил; 92 program/profile-строки; 102 field/profile-вхождения = 20 direct + 82 conditional; 62 строки-носителя = 31 предмет | Две точные строки одного предмета исполняют четыре связанные дальнобойные атаки; остальные 60 строк (30 предметов) остаются закрытыми. |
| Scroll of Dethrone | 2 profile-строки одного предмета | Обе точные A12-строки исполняются: проверка свитка, спасбросок, урон, расход и боевая запись образуют одну атомарную транзакцию. |
| Typed ABI | 4 семейства: `Break`, `Teleport`, `Resurrect`, `Restore` | Источник распознаётся и строго проверяется компилятором, но общий runtime этих четырёх семейств пока намеренно fail-closed. |
| Все действия предметов | 10 356 строк: 8 384 root + 1 346 rule + 398 recipe + 228 learn | По 5 178 строк на профиль: 4 192 root + 673 rule + 199 recipe + 114 learn. Фактический полный охват и результат прогона приведены ниже. |

Каноническая перепись `Summon` содержит 144 program/profile-строки,
соответствующие 72 правилам и 158 прямым opcode-вхождениям. На уровне
предметных действий с ними связаны 34 item/action/profile-строки (17 предметов):
20 typed-строк (10 предметов) и 14 mixed-строк (7 предметов). Кандидатами на
точное исполнение являются только 20 typed-строк; все 14 mixed сохранены в
каталоге и сертификате, но не получают частичного runtime.

A33 никогда сам по себе не вызывает `Summon`. Изученное через A33 заклинание
призыва остаётся fail-closed при последующем касте, пока для этого отдельного
пути не будет завершён равносильный точный контракт.

## Явно заблокированные семейства

Ниже перечислены осознанные границы текущего цикла, а не скрытые ограничения:

1. Все 14 mixed-строк `Summon` (7 предметов) блокируются до выбора позиции, действия и
   расхода предмета. Смешанная программа не исполняется частично.
2. Два typed-носителя Scrying блокируются до ввода и расхода: их исходный
   `CanStand` указывает не на тот blueprint, который должен быть создан.
3. Предметные составные реакции `OnSpellCast` для `Summon` сейчас блокируются
   **до** расхода действия и предмета. В активном v8 число готовых предметных
   `OnSpellCast`-маршрутов равно `0`; это не моделируется как «успешный каст с
   уже потраченным ресурсом». Такая граница защищает атомарность и не скрывает
   сами исходные строки.
4. Из 62 item/action/profile-строк `SpawnExtraProjectiles` (31 предмет) только
   две точно зафиксированные строки Arrow of Many Targets получают runtime.
   Остальные 60 строк (30 предметов) блокируются до действия, боеприпаса и урона.
5. `Break`, `Teleport`, `Resurrect` и `Restore` имеют строгий typed ABI, но не
   исполняются до появления полного runtime-контракта для мутаций мира.
6. Неизвестные, manual и mixed opcode-ветви следуют политике
   `all-reachable-opcodes-or-fail-closed`: допустимый префикс не исполняется,
   если остаётся недоказанная ветвь.
7. Формульные действия вне точных 12 лечебных строк и двух строк Dethrone
   остаются наблюдаемыми в переписи, но без доказанного контракта не пересекают
   ресурсную границу. Raw/public/proofless, sibling-use и A33-пути Dethrone тоже
   закрыты. Провал введённой проверки Интеллекта DC 15 — не блокировка: он
   штатно расходует ровно действие и свиток, но не создаёт spell-ledger,
   спасбросок или урон.
8. Ссылки на рецепты из набора 138 недопустимых и 84 заблокированных A33-строки
   сохраняются для аудита вместе с причиной, но не получают побочных эффектов.
9. Из 4 192 root-строк каждого профиля точный runtime открыт для 1 467 typed
   `readBook` / `toggleLight`. Остальные 2 725 строк на профиль, или 5 450 строк
   всего, не получают общего либо частичного исполнения и остаются fail-closed
   согласно собственным `blockers`.

Точные причины для каждой строки находятся в `blockers`, `issues`,
`unresolvedReasons` соответствующего JSON и в диагностике тестов. Поэтому
«blocked» означает проверяемый исход с нулевой мутацией, а не недоступный
пользователю контент.

## Зафиксированная проверка root-actions, `Summon`, Arrow и Dethrone

Для root-actions проверка зоны после mutation barrier охватывает точную загрузку,
чистую подготовку, единый checkpoint мира, коммит и устойчивое сохранение.
Отмена или ошибка не оставляет мутаций; откат допускает повтор того же плана,
а завершённый план нельзя воспроизвести снова. Для `Summon`, Arrow и Dethrone
расход предмета, завершение и откат реакции, последствия и представление
результата используют заранее зафиксированные зависимости; успешный коммит
сохраняет идентичность живых массивов боевого журнала и `turn.spellCasts`.
Экспорт/импорт длительности также вошёл в итоговые сертификаты.

Итоговая матрица остаётся намеренно ограниченной:

- 18 из 20 typed-строк успешно проходят точный runtime, по 9 в профилях
  `standard` и `honour`;
- 2 typed-строки Scrying блокируются из-за точного несовпадения `CanStand`;
- все 14 mixed-строк блокируются без частичного исполнения;
- готовых предметных `OnSpellCast`-композитов в активном v8 ровно `0`, поэтому
  этот путь fail-closed **до** расхода действия или предмета.

Результаты финального прогона:

| Проверка | Результат |
| --- | ---: |
| Private root hardening в `effect-engine` | 9/9 |
| Полный root-срез `standard` | 1 467/1 467; `readBook=1 218`; `toggleLight=249`; Story 10/10 |
| Root-зеркало `honour` и точные квитанции | Семантика 1 467/1 467; focused causal/durability 1/1 |
| Focused effect для `Summon` | 18/18 |
| Сертификат `bg3-summon-runtime` | 17/17 |
| Focused effect для Arrow | 32/32 |
| Сертификат `bg3-extra-projectiles-runtime` | 10/10; `TODO=0` |
| Exact-item harness Arrow | 5/5 |
| Focused effect для Dethrone | 17/17 |
| Сертификат `bg3-dethrone-runtime` | 13/13; `TODO=0` |
| Соседние Arrow-регрессии после Dethrone | 3/3 |
| Exact-item harness Dethrone | 5/5 |
| Предохранители предметов | 5/5 |
| Targeted effect `Arrow | Dethrone | Summon | A33` | 75/75 |
| Семь bounded suites каталога/runtime | 76/76; `fail=0`; `TODO=0` |
| Полная причинная матрица | 10 356/10 356: по 5 178 на `standard` и `honour`; `mismatches=0`, `auditErrors=0`, `firstMismatch=null`; 1 193,298 с |
| Полный Node-прогон, 14 файлов | 602/602; `fail=0`; `cancelled=0`; `skipped=0`; `todo=0`; 3 075,087 с |

Полный результат 602/602 получен в рабочем архиве, где рядом с production v8
сохранены исторические каталоги v3, v5, v6 и v7 для регрессий совместимости.
Публичная ветка сайта намеренно содержит только production v8; для повторения
всего исторического набора тестов эти архивные каталоги нужно предоставить
локально. Сам сайт и активный v8-каталог от них не зависят.

Финальные SHA-256 опубликованных Git-объектов этого среза:

`index.html` и `tests/effect-engine.test.mjs` до коммита имели смешанные
Windows-переносы строк и рабочие SHA-256 `136c8d7a…71a4` и
`5f4f4340…40fd6`. Git нормализовал только переносы строк в LF; нормализованный
текст побайтно совпадает, а опубликованные SHA ниже соответствуют и GitHub
source, и фактическому Pages-сайту.

| Артефакт | SHA-256 |
| --- | --- |
| `data/bg3/bg3-24532579-v8/manifest.json` | `8db8b170c1d03a75876a7c9111b33a00fd89976cc69dbd85f31762eb419c06f0` |
| `index.html` | `ff2668c0b6a8de171bdfe7725dba94cc32e55c7c5ca148979d481de4c3308f4b` |
| `tests/effect-engine.test.mjs` | `d0dbee86e31fce9dfff2938f1c64fe105ed32fe4c94a6e952aad1b2b3ccabd8c` |
| `tests/bg3-root-actions-causal.test.mjs` | `47c060702a8abbf9d64f78553d614abd91c71144ec2d978d81a041125bc9389f` |
| `tests/bg3-summon-runtime.test.mjs` | `034d42e4da037355916e46a8d51e3661776c02e193eecc81807a4702836a11fb` |
| `tests/bg3-action-boundary-causal.test.mjs` | `08b4cf4e15e7d5cd5e0d5a12d77c472bef20e70f621549d36305ce17ca4e8942` |
| `tests/bg3-item-guards.test.mjs` | `6e43eda38281c904dff5317c02d3b17de7d180b9ca3cb70d287ad96157206945` |
| `tests/bg3-learn-spell-readiness.test.mjs` | `2200601cf045f2ff5772e131081f7ccfcb65ce6a181d17f770683f80d413a0c2` |
| `tests/bg3-extra-projectiles-runtime.test.mjs` | `ed42364acdc51cc88e8f55b8ce706582ea8db50b636ff3b259844756f16e3312` |
| `tests/bg3-dethrone-runtime.test.mjs` | `e177bff14b5d59df055d0049ea16f892f8c83d064f371e4cde8e643c674d2703` |

## Зафиксированный срез корневых `readBook` и `toggleLight`

Точная перепись содержит 2 934 профильных действия с раздельными идентификаторами:
по 1 467 на `standard` и `honour`. В каждом профиле это 1 218 `readBook` и 249
`toggleLight` на 1 466 предметах. Нормализованные семантики двух профилей
совпадают без расхождений, хотя их use/root-ID намеренно не пересекаются.

Весь срез `standard` проходит production-маршрут
`bg3ItemProgramOpen → private exact modal → castConfirm`. Загрузчик материализует
нужный профиль и закреплённый артефакт, включая холодную загрузку байтов без
перехода к общему fallback. Подготовка остаётся gameplay-pure: отмена, отсутствие
записи и самовосстановление точного book-cache не пересекают ресурсную границу.
Успех создаёт один коммит; `readBook` записывает точный `BookId` и только
связанные рецепты без дублей, а `toggleLight` использует детерминированный
source-unique UID, поэтому независимые источники света не выключают друг друга.

На каждый профиль приходится 10 точных Story-связей. Для `standard` две
исполняются из инвентаря, четыре — через размещённую сцену, а ещё четыре
доказуемо блокируются прямым placement override без мутации и оплаты. `Honour`
подтверждает полное семантическое зеркало и отдельные точные квитанции
Story/read, light, rollback, replay и долговечности.

Книга, Story/scene, рецепты, свет, боевой ресурс и журнал входят в общий
checkpoint мира. Поздняя ошибка откатывает их вместе, тот же закрытый план можно
повторить один раз, а успешный replay отвергается без нового коммита. Синхронный
локальный world-snapshot служит write-ahead границей до завершения semantic burn;
если он недоступен, действие откатывается до записи в primary storage. Поле
`snapshotRevision` позволяет загрузчику выбрать более свежую из primary/local
копий, а асинхронный отказ primary сохраняет признак несохранённого мира.
Представление результата выполняется отдельно и не даёт враждебному UI-setter
изменить уже проверенное устойчивое состояние.

## Зафиксированный срез Arrow of Many Targets

Перепись `SpawnExtraProjectiles` содержит 46 правил, 92 program/profile-строки
и 102 field/profile-вхождения (`20 direct + 82 conditional`). На уровне
предметов это 62 строки, 31 предмет и 26 первичных правил. Частный runtime
разрешён только для двух точно связанных строк Arrow of Many Targets; остальные
60 строк (30 предметов) по-прежнему блокируются до оплаты.

Игрок явно и по порядку выбирает четыре разные живые цели и вводит физические
броски каждой атаки. MAIN_HAND расходует одно действие, OFF_HAND — одно бонусное
действие. Попадание или промах не обрывает цепочку, но при промахе урон равен
нулю. При попадании первая цель получает полный базовый урон, каждая дочерняя —
округлённую вниз половину. Коммит расходует ровно одну Arrow, не трогает обычные
боеприпасы и сохраняет выбранное оружие. Поздняя
ошибка полностью откатывает HP, действие, предмет, журнал и идентичности; тот же
закрытый proof можно один раз повторить, а успешный replay отвергается.

Сертификат `bg3-extra-projectiles-runtime` завершён: 10/10 тестов, `TODO=0`.

## Зафиксированный срез Scroll of Dethrone

Dethrone охватывает ровно две profile-строки одного предмета: `standard` и
`honour`. Канонический агрегат двух исходных строк занимает 997 байт и имеет
SHA-256 `7d6b39537450ae81c842eae195145ef859ff5de0559da56ecaebf05935e5911f`.

Подтверждённый Wizard с уровнем класса 9 и выше не проходит проверку свитка;
при уровне класса ниже 9 игрок вводит d20 Интеллекта против DC 15. Персонаж без
подтверждённого класса Wizard отклоняется до оплаты. Провал проверки атомарно расходует одно
действие и свиток, но не создаёт spell-ledger, спасбросок или урон. При успешном
касте выбирается одна живая цель на явной дистанции не более 30, вводится её
спасбросок Телосложения против DC 18 и один итог `10d6 = D`. Провал спасброска
наносит `D + 20`, успех — `floor((D + 20) / 2)`. Урон зафиксирован как
Necrotic + Magical, слот заклинания не расходуется, а успешный каст добавляет
точную боевую запись заклинания 5-го уровня.

Действие, свиток, HP и spell-ledger входят в один checkpoint с
rollback/retry/replay; опасные реакции, неподтверждённые наблюдатели и
неоднозначные соседние маршруты блокируются до оплаты. Сертификат
`bg3-dethrone-runtime` завершён: 13/13 тестов, `TODO=0`.

## Локальный запуск интерфейса без внешнего пакета

Используется поставляемый с рабочей средой Node.js 24.19.0:

```powershell
$Repo = 'C:\Users\admin\.codex\visualizations\2026\08\12\019ff62f-b4c0-72f2-917b-9630506f8cc9\dnd-world'
$Node = 'C:\Users\admin\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe'
Set-Location $Repo
& $Node --version
```

Чтобы браузер мог загружать JSON-сегменты через `fetch`, проект лучше открыть
через локальный HTTP, а не как `file://`. Следующий сервер использует только
стандартную библиотеку Node и ничего не устанавливает:

```powershell
$ServerSource = @'
const http = require('node:http');
const fs = require('node:fs');
const path = require('node:path');
const root = process.cwd();
const types = {'.html':'text/html; charset=utf-8','.json':'application/json; charset=utf-8','.webp':'image/webp','.png':'image/png','.svg':'image/svg+xml'};
http.createServer((request, response) => {
  const pathname = decodeURIComponent(new URL(request.url, 'http://127.0.0.1').pathname);
  const relative = pathname === '/' ? 'index.html' : pathname.replace(/^\/+/, '');
  const filename = path.resolve(root, relative);
  if (filename !== root && !filename.startsWith(root + path.sep)) {
    response.writeHead(403); response.end('Forbidden'); return;
  }
  fs.stat(filename, (error, stat) => {
    if (error || !stat.isFile()) { response.writeHead(404); response.end('Not found'); return; }
    response.setHeader('Content-Type', types[path.extname(filename).toLowerCase()] || 'application/octet-stream');
    fs.createReadStream(filename).pipe(response);
  });
}).listen(8000, '127.0.0.1', () => console.log('Откройте http://127.0.0.1:8000'));
'@
& $Node -e $ServerSource
```

Остановить сервер можно `Ctrl+C`. В другом окне PowerShell адрес открывается
командой `Start-Process 'http://127.0.0.1:8000'`.

## Воспроизводимые проверки

Быстрые критические наборы:

```powershell
$Repo = 'C:\Users\admin\.codex\visualizations\2026\08\12\019ff62f-b4c0-72f2-917b-9630506f8cc9\dnd-world'
$Node = 'C:\Users\admin\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe'
Set-Location $Repo

& $Node --test --test-concurrency=1 "$Repo\tests\bg3-summon-runtime.test.mjs"
& $Node --test --test-concurrency=1 "$Repo\tests\bg3-extra-projectiles-runtime.test.mjs"
& $Node --test --test-concurrency=1 "$Repo\tests\bg3-dethrone-runtime.test.mjs"
& $Node --test --test-concurrency=1 "$Repo\tests\bg3-learn-spell-readiness.test.mjs"
& $Node --test --test-concurrency=1 --test-name-pattern="BG3 private root" "$Repo\tests\effect-engine.test.mjs"
& $Node --test --test-concurrency=1 "$Repo\tests\effect-engine.test.mjs"
```

Полный причинный сертификат root-actions длительный и запускается отдельно:

```powershell
& $Node --test --test-concurrency=1 "$Repo\tests\bg3-root-actions-causal.test.mjs"
```

Только 34 item/action/profile-строки `Summon` (17 предметов) в общей причинной матрице:

```powershell
$env:BG3_ACTION_SUMMON_ONLY = '1'
try {
  & $Node --test --test-concurrency=1 "$Repo\tests\bg3-action-boundary-causal.test.mjs"
} finally {
  Remove-Item Env:BG3_ACTION_SUMMON_ONLY -ErrorAction SilentlyContinue
}
```

Точные предметы Arrow и Dethrone можно прогнать отдельно тем же harness:

```powershell
$ArrowItem = 'bg3:item:rt:b11b3f7f-d8ec-41db-93b9-24474aea31e3:stats:T0JKX0Fycm93T2ZSaWNvY2hldA'
$DethroneItem = 'bg3:item:rt:b2e1168a-021d-4a81-a041-6d2e1421a1fb:stats:VU5JX0xPV19EZXRocm9uZVNjcm9sbA'
foreach ($ItemId in @($ArrowItem, $DethroneItem)) {
  $env:BG3_ACTION_ITEM = $ItemId
  & $Node --test --test-concurrency=1 "$Repo\tests\bg3-action-boundary-causal.test.mjs"
}
Remove-Item Env:BG3_ACTION_ITEM -ErrorAction SilentlyContinue
```

Полный корпус 10 356 item/action/profile-строк длительный, но не требует
сетевого доступа:

```powershell
Remove-Item Env:BG3_ACTION_SUMMON_ONLY -ErrorAction SilentlyContinue
Remove-Item Env:BG3_ACTION_PROFILE -ErrorAction SilentlyContinue
Remove-Item Env:BG3_ACTION_ITEM -ErrorAction SilentlyContinue
Remove-Item Env:BG3_ACTION_USE -ErrorAction SilentlyContinue
& $Node --test --test-concurrency=1 "$Repo\tests\bg3-action-boundary-causal.test.mjs"
```

Весь Node-набор проекта:

```powershell
$TestFiles = Get-ChildItem "$Repo\tests" -File |
  Where-Object Name -Match '\.test\.(mjs|cjs)$' |
  Sort-Object Name |
  ForEach-Object FullName
& $Node --test --test-concurrency=1 @TestFiles
```

Независимая проверка указателя и хеша каталога:

```powershell
$CurrentPath = "$Repo\data\bg3\current.json"
$Current = Get-Content -Raw $CurrentPath | ConvertFrom-Json
$ManifestPath = Join-Path "$Repo\data\bg3" $Current.manifest
$ActualSha256 = (Get-FileHash $ManifestPath -Algorithm SHA256).Hash.ToLowerInvariant()
[pscustomobject]@{
  Catalog = $Current.catalogVersion
  Manifest = $ManifestPath
  ExpectedSha256 = $Current.manifestSha256
  ActualSha256 = $ActualSha256
  Matches = $ActualSha256 -eq $Current.manifestSha256
}
```

Проверка пробелов и случайных правок перед передачей:

```powershell
git diff --check
git status --short
```

## Файлы Node-тестов

- `tests/bg3-action-boundary-causal.test.mjs` — все 10 356 действий и
  специальные фильтры профиля/предмета/Summon/Arrow/Dethrone;
- `tests/bg3-books.test.mjs` — книги;
- `tests/bg3-catalog-selection.test.mjs` — детерминированная выборка каталога;
- `tests/bg3-catalog.test.mjs` — манифест, сегменты и каталог;
- `tests/bg3-every-item.test.mjs` — полный обход предметов;
- `tests/bg3-extra-projectiles-runtime.test.mjs` — замороженная перепись и
  частный причинный runtime Arrow;
- `tests/bg3-dethrone-runtime.test.mjs` — точный источник и причинный runtime
  двух профильных строк Scroll of Dethrone;
- `tests/bg3-item-guards.test.mjs` — входные и ресурсные предохранители;
- `tests/bg3-learn-spell-readiness.test.mjs` — сертификат 228 A33-строк;
- `tests/bg3-root-actions-causal.test.mjs` — 1 467 root-actions на профиль,
  семантическое зеркало `standard` / `honour`, 10 Story-связей, а также
  rollback/retry/replay и устойчивое сохранение;
- `tests/bg3-story.test.mjs` — сюжетные данные и маршруты;
- `tests/bg3-summon-runtime.test.mjs` — перепись и частный runtime Summon;
- `tests/effect-engine.test.mjs` — основная спецификация движка и регрессии;
- `tests/effect-parser.test.cjs` — совместимый парсер эффектов.

`tests/bg3-catalog-selection.mjs` — общий помощник выборки; он не является
самостоятельным `*.test.*`, но входит в проверяемую реализацию тестов каталога.

## Экспорт, импорт и редактирование

### Кампания и пользовательские записи

Кнопка **«Выгрузить летопись»** создаёт `dnd-world-letopis.json`. Это обычный
читаемый UTF-8 JSON с персонажами, журналом, боем, предметами пользователя,
заклинаниями, способностями, противниками, сценами и `catalogRefs`. Импорт
выполняется соседней кнопкой интерфейса.

Полные 10 284 записи BG3 намеренно не дублируются в каждом экспорте: они уже
лежат в `data/bg3/...`, а `catalogRefs` сохраняет версию, профиль и хеш. Это
ленивая загрузка для размера и воспроизводимости, а не скрытие данных.

Рекомендуемый порядок ручной работы:

1. Сначала выгрузить летопись и сохранить резервную копию.
2. Редактировать копию JSON редактором, сохраняющим UTF-8 и валидный JSON.
3. Не добавлять в экспорт одноразовые proof/token/permit: движок всё равно не
   считает сериализованный объект полномочием на коммит.
4. Не менять `catalogRefs`, если соответствующая версия каталога не была
   действительно создана и проверена.
5. Импортировать копию и выполнить нужное действие заново; итоговое состояние
   снова можно выгрузить без потери пользовательских данных.

### Каталог и движок

Поле `immutable: true` в манифесте — правило версионирования, не файловый
запрет. Для содержательного изменения каталога безопаснее:

1. сохранить `bg3-24532579-v8` как воспроизводимый предыдущий снимок;
2. собрать новую версию из stage1/stage2 либо скопировать снимок под новым
   номером для контролируемой правки;
3. обновить связанные сегменты, индексы, counts, source fingerprints и
   capability-контракты как единый набор;
4. пересчитать SHA-256 нового `manifest.json` и только затем изменить
   `data/bg3/current.json`;
5. прогнать тесты каталога, targeted runtime-тесты и полный причинный корпус;
6. после успешного прогона записать отдельные SHA-256 движка и его сертификата
   в этот журнал.

Редактирование только одного JSON при старом манифесте корректно будет
воспринято как рассогласование и заблокирует исполнение до расхода ресурсов.
Это делает ошибку видимой и обратимой; файл при этом остаётся полностью
доступен владельцу.

## Фиксация завершённого цикла

Срезы root `readBook` / `toggleLight`, `Summon`, Arrow of Many Targets и Scroll
of Dethrone удовлетворили своим воротам фиксации. Сертификаты подтверждают
точный источник, введённые игроком кости там, где они требуются, единичный
расход, последствия, rollback/retry/replay, устойчивое сохранение и отсутствие
скрытого `Math.random`; неоднозначные соседние строки отклоняются до оплаты.
Независимая финальная проверка root-среза не выявила замечаний уровней P0 или P1.
После последней правки полный причинный корпус прошёл 602/602 без ошибок,
отмен, пропусков и TODO; обе профильные матрицы прошли все 10 356 строк без
расхождений. Фактические результаты и вычисленные после прогона хеши записаны
выше.

Процессный и итоговый контент остаются полностью доступными по указанным путям.
Integrity-проверки продолжают предотвращать только неподтверждённую мутацию;
они не ограничивают чтение, экспорт, редактирование или независимую проверку
владельцем.
