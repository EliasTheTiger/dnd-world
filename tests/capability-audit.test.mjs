import assert from 'node:assert/strict';import {spawnSync} from 'node:child_process';import test from 'node:test';
test('capability audit inventories handlers, hidden catches, catalogs and rulesets',()=>{const result=spawnSync(process.execPath,['scripts/audit-capabilities.mjs'],{encoding:'utf8'});assert.equal(result.status,0,result.stderr);const report=JSON.parse(result.stdout);assert.equal(report.schemaVersion,'dnd-world-capability-audit/1');assert.ok(report.summary.inlineUiAttributes>0);assert.ok(report.summary.testFiles>0);assert.ok(Array.isArray(report.missingHandlers));assert.deepEqual(report.activeRulesets.map(row=>row.id),['dnd5e-2014-local','bg3-5e-2014-adaptation']);assert.equal(report.catalogClaims.find(row=>row.id==='local-spells').expectedMode,'unknown');});
